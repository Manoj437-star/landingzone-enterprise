trigger:
  branches:
    include:
      - feature/*
      - main

pool: Default

steps:

- checkout: self

- powershell: |
    Write-Host "Repository downloaded successfully"

    Get-Location

    Get-ChildItem -Recurse
  displayName: "Checkout Repository"